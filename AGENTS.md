# AGENTS.md — 給 AI Agent 的 repo 導覽

這份檔案是寫給**讀到這個 repo 的 AI agent** 看的，不是教學內容。
使用者要看的是 [README.md](README.md)。

---

## 這個 repo 是什麼

**OpenCode 懶人包**：一組給台灣國中小老師的設定教學。每一包都是一份 `SKILL.md`，內容是「請 agent 照著執行」的步驟——偵測環境、安裝工具、寫設定檔、驗證結果、回報。

對象是**零程式基礎的老師**。所以：

- 全程繁體中文。
- 遇到需要人手動處理的地方（點瀏覽器同意、輸入驗證碼、需要管理員權限），**停下來講清楚要做什麼，等使用者回覆**，不要自己硬闖。
- 不要幫使用者選付費方案，不要代填任何金鑰或密碼。
- Windows 一律用 PowerShell；**不要用 `&&` 串指令**（PowerShell 5.1 會語法錯誤），用 `;`。

---

## 目錄結構

```
README.md                     ← 給人看的入口
CHANGELOG.md                  ← 唯一的版本紀錄
AGENTS.md                     ← 你正在讀的這份
SKILL.md                      ← 安裝入口：列清單 → 問使用者 → 依序安裝
skills/<編號>-<名稱>/SKILL.md  ← 主線 11 包，每包一個資料夾
extras/<名稱>/SKILL.md         ← 加碼 2 包，非主線
```

**每一包的內容只有一個真相來源：`skills/<名稱>/SKILL.md`。**
根目錄沒有、也不該再出現長版教學 MD（v1 曾經有，兩份會漂移，已移除）。

---

## 編號怎麼讀

編號依「作品階梯」排，**編號 = 建議的學習順序**，不是流水號：

| 階梯 | 包 | 意義 |
|------|----|------|
| 地基 | `00-env-setup`、`01-connect-model` | 沒做完這兩包，後面全部跑不動 |
| 內部工具 | `02-file-toolkit` | 讓 agent 長出手腳（本機處理檔案、影音、語音） |
| 外部工具 | `03-notebooklm`、`04-obsidian` | 把外面的服務接進來 |
| 資料庫 | `05-sheets-gas` ／ `06-supabase` | 讓成品記得住東西 |
| API | `09-groq-api`、`10-netlify` | 呼叫外部服務，並把金鑰藏在後端 |
| 跨電腦 | `07-github`、`08-workflow-skills` | 換一台電腦也接得回來 |

> ⚠️ **`00` 裝完不等於能用。** `00` 只裝工具，**`01` 才接上模型**。
> 使用者如果說「裝完了但 OpenCode 不回話」，答案幾乎一定是「還沒做 `01-connect-model`」，不是裝壞了。

> 📌 **交叉引用一律用資料夾名稱**（例如 `` `07-github` ``），不要只寫「#02」這種純編號。
> 編號在 v1→v2 之間搬動過，純編號的引用會過期。

---

## 使用者這樣說 → 你去哪一包

| 使用者說 | 對應 |
|---------|------|
| 「裝 OpenCode」「環境建置」「從零開始設定」 | `00-env-setup` |
| 「裝完不能用」「沒有模型」「設定 API key」「要用免費模型」 | `01-connect-model` |
| 「讓 AI 幫我做 Word／Excel／PPT／PDF」「做 QR Code」「下載影片」「轉字幕音檔」「文字轉語音」 | `02-file-toolkit` |
| 「連接 NotebookLM」「自動做簡報／資訊圖表」 | `03-notebooklm` |
| 「連接 Obsidian」「第二大腦」「讀我的筆記」 | `04-obsidian` |
| 「用 Google 試算表存資料」「Apps Script」「表單後端」 | `05-sheets-gas` |
| 「語音轉字幕」「上字幕」「Whisper」「逐字稿」「清逐字稿」 | `09-groq-api` |
| 「部署」「上線」「做成網頁」「藏 API 金鑰」 | `10-netlify` |
| 「資料庫」「存資料」「文字雲」「即時」 | 非即時→`05-sheets-gas`；即時→`06-supabase` |
| 「連接 GitHub」「clone 懶人包」「怕弄丟」「換電腦拿回檔案」 | `07-github` |
| 「開工」「收工」「初始化專案」「跨電腦接續」 | `08-workflow-skills` |
| 「連接 Firebase」 | `extras/firebase`（替代方案；預設請用 `06-supabase`）|
| 「瀏覽器自動化」「讓 AI 操作網頁」 | `extras/browser` |
| 「生圖」「畫圖」 | 不在本 repo。指向 <https://github.com/mathruffian-dot/opencode-draw-free> |

判斷不出來就**直接問使用者**，不要猜著裝。

---

## `skills/` 與 `extras/` 的差別

| | `skills/` | `extras/` |
|---|-----------|-----------|
| 定位 | 三天研習的主線 | 加碼路線，不在研習流程裡 |
| 內容狀態 | v2 重新查證或重寫過 | 沿用 v1，未重寫 |
| 主動推薦 | 可以依階梯建議下一包 | **不要主動推薦**，使用者明確要求才做 |
| 一鍵安裝 | `npx skills add … --skill <資料夾名>` | 可能抓不到，改為直接讀檔照做 |

---

## 🔴 兩個會花錢或會出事的紅線

**`10-netlify`：沒有使用者明確同意，不准執行 `netlify deploy --prod`。**
Netlify 免費方案是點數制，一次正式部署扣 15 點、每月只有 300 點。草稿部署（不加 `--prod`）是 0 點。**預設一律走草稿**，要正式發布必須問過使用者。

**`09-groq-api`：語音會送到雲端，上傳前必須先問。**
含學生聲音、家長談話、輔導或個案內容的錄音一律不上傳。這條在該包裡是硬性流程，不是建議。

---

## ⚠️ 最重要的一條：一次只做使用者要求的那一包

使用者說「幫我裝 NotebookLM」，就**只裝 NotebookLM**。

裝完之後：回報結果 → 說明下一包建議是什麼 → **停下來等使用者決定**。

不要因為「反正都在同一個 repo」「順便一起裝比較快」就連續往下裝。理由：

1. 每一包都會改到使用者電腦上的設定檔（`~/.config/opencode/opencode.json`）、安裝全域工具。連續動手，出錯時**分不出是哪一包壞的**。
2. 有些包需要使用者本人操作（登入、授權、貼金鑰）。你自作主張往下跑，只會卡在半路，留下一堆做到一半的設定。
3. 研習現場是跟著講師的進度走的。你多裝的每一包，都是講師台上要收拾的狀況。

**唯一的例外**：使用者明確說「全部」「一次裝完」。這時照根目錄 `SKILL.md` 的流程，依編號順序一包一包做，**每包做完都要回報**，遇到失敗就停下來問，不要跳過去繼續。

---

## 修改這個 repo 的時候

如果使用者要你**編輯**這個 repo（而不是照著它安裝）：

- 版本紀錄只寫在 `CHANGELOG.md`。**不要**在各技能檔裡新增「版本：vX」或更新紀錄表格——v1 就是這樣散成九份、然後對不起來的。
- 懶人包清單只維護 `README.md` 那一張表。新增一包時，`README.md`、`SKILL.md`、本檔的對照表要同步更新。
- 技能檔內要示範 frontmatter 時，**一定要用程式碼圍籬包住**（例如四個反引號的 ````md` 區塊），並註明「不要把圍籬本身寫進檔案」。沒包住會讓 agent 照抄出格式壞掉的檔案。
- 專案藍圖檔名一律大寫 `AGENTS.md`。小寫在 Mac／Linux 會發生「檔案在、agent 卻讀不到」。
- 這是**公開** repo：不要寫入任何個人資料夾路徑、帳號、金鑰，或維護者的內部工作筆記。
